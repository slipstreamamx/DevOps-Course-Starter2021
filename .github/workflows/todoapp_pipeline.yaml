name: Continuos Integration
on: [push]

jobs:
  build:
    name: build and test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout files
        uses: actions/checkout@v2
      - name: Build docker
        uses: docker build --target testing -- tag todo-app:testing .
      